# Invalid fixture: locale `nm` has an empty body (no template), which must fail loudly
# instead of emitting an empty record. Locked by
# __snapshots__/invalid-unknown-template-error.txt.
org:
  username: broken-org
templates:
  good-template:
    folder-name: ✏️{ locale.key }✏️-legislation
locales:
  nm:
  ok:
    template: good-template
    name: Okay
