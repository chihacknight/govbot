# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
*.swp
*.swo
*.iml
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Project specific
test_output/
*.log
sample_data/*

# Project documentation (tracked for history — see project_docs/)
WORKFLOW_PREFERENCES.md
actions/scrape/session-notes-*.md
actions/scrape/combined-status-*.md
actions/scrape/session-calendar-*.md
actions/scrape/2026_Session_Calendar.pdf

# Temporary files
*.tmp
*.temp
file_structure.md

# Analysis outputs
llm_analysis_results.json
bill_tracker_report.json
wyoming_bill_tracker_report.json
*_llm_analysis_*.json

# Data outputs (keep structure but ignore content)
data_output/data_processed/*/logs/
data_output/data_processed/*/files/
data_output/data_not_processed/
data_output/event_archive/

# Cache and temporary files
.cache/
.pytest_cache/
.coverage
htmlcov/

# Environment files
.env
.env.local
.env.*.local

# Jupyter notebooks
.ipynb_checkpoints/
*.ipynb

# Backup files
*.bak
*.backup
*~

# Testing - Sample data (too large for repo)
testing/sample_data/usa-complete/
testing/sample_data/usa-latest/
testing/sample_data/comprehensive/
testing/incremental/test_input/
testing/incremental/test_output/
docs/action_logs/*

**/tmp/*

# VHS generated recordings (stored as CI artifacts, not in repo)
*.gif

# Rust / Cargo
target/
**/target/
# Save .cargo/config if used for config, not secrets

# Rust build output
*.rs.bk

# Rust/Cargo temporary and backup files
*.rs.bk
*.rlib

sample_data/*

# Claude Code local tool config (personal permission allowlists, not shared project config)
.claude/



reviews/

# Vendored legislator sponsor photos + manifest: build artifacts fetched during
# the Pages deploy (scripts/fetch_sponsor_photos.py) and published with the
# site, never committed — so no photo dump lands in git history.
docs/src/dashboard/assets/legislators/
docs/src/dashboard/legislator_images.json

# Illinois bill synopses ("What this bill is about"): a build artifact extracted
# from official ILGA full text during the Pages deploy (scripts/build_il_summaries.py),
# carried between runs by actions/cache and published with the site — not committed.
docs/src/dashboard/il_summaries.json
